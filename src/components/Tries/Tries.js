import React, {Fragment} from 'react';

const Tries = (props) => {
    return (
        <Fragment>
            <div className="tries">Tries: {props.tries}</div>
        </Fragment>
    )
};

export default Tries;